# A React Project simulating a basic web application with a purchasing UI
Capstone Project I did in my React BootCamp using the UseContext hook, React Router DOM, fetching calls from the UseEffect hook and creating a shopping cart where users can buy pictures of their choice. 
